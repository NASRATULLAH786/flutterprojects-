class LeavePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Leave Management'),
      ),
      body: Center(
        child: Text('Apply and manage leave requests here!'),
      ),
    );
  }
}
